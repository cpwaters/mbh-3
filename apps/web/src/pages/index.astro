---
import Base from '../layouts/Base.astro';
---

<Base title="MyBackHaul — fill your empty return legs">
  <main class="min-h-screen bg-gradient-to-br from-blue-50 via-white to-blue-50">
    <div class="max-w-5xl mx-auto px-4 py-16 sm:py-24">
      <div class="text-center">
        <div class="inline-flex items-center gap-2 mb-8">
          <img
            src="/android-chrome-384x384.png"
            alt="MyBackHaul logo"
            width="384"
            height="384"
            class="w-9 h-9"
          />
          <span class="text-2xl font-bold text-gray-900">MyBackHaul</span>
        </div>

        <h1 class="text-4xl sm:text-6xl font-bold text-gray-900 leading-[1.05] tracking-tight mb-6">
          Fill your empty <span class="text-blue-600">return legs</span>.
        </h1>
        <p class="text-lg sm:text-xl text-gray-600 max-w-2xl mx-auto mb-10">
          MyBackHaul matches distributors' loads with hauliers who have spare capacity on the way
          back — with a proof of delivery you can rely on, captured even where there's no signal.
        </p>

        {/* Two doors, because the page sells to two trades. Each carries the
            intent into the matching sign-up, which pre-selects the kind of
            company they are setting up. */}
        <div class="flex flex-col sm:flex-row items-stretch justify-center gap-3 max-w-xl mx-auto">
          <a
            href="/app/signup/shipper"
            class="flex-1 inline-flex flex-col items-center justify-center gap-1 bg-blue-600 text-white px-7 py-4 rounded-lg font-semibold shadow-lg shadow-blue-600/20 hover:bg-blue-700 transition-colors"
          >
            <span class="text-base">I ship loads</span>
            <span class="text-xs font-normal text-blue-100">Post backhauls to hauliers</span>
          </a>
          <a
            href="/app/signup/carrier"
            class="flex-1 inline-flex flex-col items-center justify-center gap-1 bg-gray-900 text-white px-7 py-4 rounded-lg font-semibold shadow-lg shadow-gray-900/20 hover:bg-gray-800 transition-colors"
          >
            <span class="text-base">I haul loads</span>
            <span class="text-xs font-normal text-gray-300">Fill your empty return legs</span>
          </a>
        </div>

        <p class="text-gray-600 mt-5">
          Already with us?
          <a href="/app" class="font-semibold text-blue-700 hover:text-blue-900">Sign in</a>
          <span class="text-gray-400 mx-2">·</span>
          <a href="/guide" class="font-semibold text-blue-700 hover:text-blue-900">How it works</a>
        </p>

        {/* Installing it. A driver wants this on the home screen next to the
            tachograph apps, not behind a browser tab — it opens full screen and
            the proof-of-delivery capture keeps working with no signal. Chrome
            and Edge hand us a prompt to fire; iOS has no such API, so there the
            honest thing is to name the two taps. Both start hidden and are
            revealed by the script below, so nobody is told to install an app
            they are already running. */}
        <div id="install" hidden class="mt-8">
          <button
            id="install-button"
            type="button"
            hidden
            class="inline-flex items-center gap-2 bg-white text-gray-900 border border-gray-300 px-5 py-2.5 rounded-lg font-semibold shadow-sm hover:bg-gray-50 transition-colors"
          >
            <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v12m0 0l-4-4m4 4l4-4M4 20h16" />
            </svg>
            Install the app
          </button>

          <p id="install-ios" hidden class="text-sm text-gray-600 max-w-sm mx-auto">
            <span class="font-semibold text-gray-900">Install on this device:</span>
            tap Share, then <span class="font-semibold text-gray-900">Add to Home Screen</span>.
          </p>

          <p class="text-xs text-gray-500 mt-2">
            Works offline. Loads and proof of delivery stay on the device until there's signal.
          </p>
        </div>
      </div>

      <div class="grid sm:grid-cols-3 gap-5 mt-16 sm:mt-24">
        <div class="bg-white rounded-2xl shadow-md border border-gray-100 p-6">
          <div class="w-11 h-11 rounded-xl bg-blue-100 text-blue-600 flex items-center justify-center mb-4">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4" /></svg>
          </div>
          <h3 class="text-lg font-bold text-gray-900 mb-1">Post a load</h3>
          <p class="text-gray-600 text-sm">Distributors post backhaul loads in seconds; the route is worked out automatically.</p>
        </div>
        <div class="bg-white rounded-2xl shadow-md border border-gray-100 p-6">
          <div class="w-11 h-11 rounded-xl bg-blue-100 text-blue-600 flex items-center justify-center mb-4">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 17a2 2 0 11-4 0 2 2 0 014 0zM20 17a2 2 0 11-4 0 2 2 0 014 0z" /><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16V6a1 1 0 00-1-1H4a1 1 0 00-1 1v10a1 1 0 001 1h1m8-1a1 1 0 01-1 1H9m4-1V8h4l3 3v5a1 1 0 01-1 1h-1" /></svg>
          </div>
          <h3 class="text-lg font-bold text-gray-900 mb-1">Find a backhaul</h3>
          <p class="text-gray-600 text-sm">Hauliers browse loads on the way back and accept the one that fits their route.</p>
        </div>
        <div class="bg-white rounded-2xl shadow-md border border-gray-100 p-6">
          <div class="w-11 h-11 rounded-xl bg-green-100 text-green-600 flex items-center justify-center mb-4">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
          </div>
          <h3 class="text-lg font-bold text-gray-900 mb-1">Proof of delivery</h3>
          <p class="text-gray-600 text-sm">Capture a photo, signature and recipient on delivery — it works with no signal and syncs later.</p>
        </div>
      </div>

      <p class="text-center text-gray-500 text-sm mt-16">
        Built for UK haulage. Proof of delivery is recorded on the driver's device the moment it
        happens and delivered when signal returns.
      </p>
    </div>
  </main>

  <script>
    import { isIOS, isStandalone } from '../lib/platform';

    // Progressive: if none of this runs, the page is exactly as it was.
    const wrap = document.getElementById('install');
    const button = document.getElementById('install-button');
    const ios = document.getElementById('install-ios');

    // Already launched from the home screen — nothing to offer.
    if (wrap && button && ios && !isStandalone()) {
      let prompt: (Event & { prompt(): Promise<void> }) | null = null;

      // Chromium fires this when the app qualifies (manifest + service worker
      // + https). Holding onto the event is what lets us put the prompt behind
      // our own button instead of the browser's mini-infobar.
      window.addEventListener('beforeinstallprompt', (event) => {
        event.preventDefault();
        prompt = event as Event & { prompt(): Promise<void> };
        wrap.hidden = false;
        button.hidden = false;
      });

      button.addEventListener('click', () => {
        if (!prompt) return;
        void prompt.prompt();
        // The event is single-use: a second prompt() throws.
        prompt = null;
        button.hidden = true;
      });

      window.addEventListener('appinstalled', () => {
        wrap.hidden = true;
      });

      // Safari on iPhone/iPad can install, but only by hand. Chrome and
      // Firefox on iOS are Safari underneath and cannot install at all, so
      // telling them to tap Share would be a lie.
      if (isIOS() && !/CriOS|FxiOS/.test(window.navigator.userAgent)) {
        wrap.hidden = false;
        ios.hidden = false;
      }
    }
  </script>
</Base>
