# Copy to terraform.tfvars (gitignored) and fill in. Never commit the real one.
billing_account = "XXXXXX-XXXXXX-XXXXXX"
alert_email     = "you@example.com"
# project_id, region, github_repo, site_url, budget_amount_gbp have defaults
# in variables.tf — override here only if they differ.
